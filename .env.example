# Server-only — jangan prefix PUBLIC_, jangan commit nilai sungguhan
RESEND_API_KEY=
TURNSTILE_SECRET_KEY=

# Server-only — Fase 1: database, autentikasi, upload
DATABASE_URL=
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=
ADMIN_EMAIL=
ADMIN_INITIAL_PASSWORD=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Server-only — Fase 3.5: unique-visitor hash (openssl rand -hex 32)
VISITOR_HASH_SECRET=

# Server-only — Fase 4: autentikasi cron backup (openssl rand -hex 32)
# Tambahkan manual di Vercel Dashboard — TIDAK dibuat otomatis oleh Vercel
# walau cron didefinisikan di vercel.json.
CRON_SECRET=

# Public — dipakai widget client-side, aman diekspos ke browser
PUBLIC_TURNSTILE_SITE_KEY=
